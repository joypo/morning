package ${basePackage}.dao;

import ${basePackage}.base.core.Mapper;
import ${basePackage}.domain.${modelNameUpperCamel};

/**
 * @author ${author}
 * @date ${date}
 * @description ${table.comment}Dao
 */
public interface ${modelNameUpperCamel}Dao extends Mapper<${modelNameUpperCamel}> {
}