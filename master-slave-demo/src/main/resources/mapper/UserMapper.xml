<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.master.slave.dao.UserDao">
    <insert id="save" parameterType="com.example.master.slave.entity.User">
        insert into tb_user (user_name,pwd) value (#{userName},#{pwd})
    </insert>
</mapper>